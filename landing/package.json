{
  "name": "maphpro3d-landing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "prebuild": "node -e \"const fs=require('fs'); const p='public/logo.png'; const up='../public/logo.png'; fs.mkdirSync('public',{recursive:true}); try{fs.copyFileSync(up,p)}catch(e){}\"",
    "build": "npm run prebuild && next build",
    "start": "next start -p 3001"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.0",
    "postcss": "^8.5.0",
    "tailwindcss": "^4.2.0",
    "typescript": "^5.0.0"
  }
}
